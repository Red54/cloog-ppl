/* Generated from ./lux.cloog by CLooG  64 bits in 0.05s. */
if (M >= 2) {
  for (l=2;l<=M;l++) {
    S1(i = 1,j = 1,k = M) ;
  }
}
for (i=2;i<=M-1;i++) {
  for (j=1;j<=i-1;j++) {
    for (k=j+1;k<=M;k++) {
      if ((j <= k-1) && (k <= M)) {
        if ((i >= j+1) && (i <= M-1)) {
          S2(l = k,m = i) ;
        }
      }
    }
  }
  if ((i >= 2) && (i <= M-1)) {
    for (l=i+1;l<=M;l++) {
      S1(j = i,k = M) ;
    }
  }
}
if (M >= 2) {
  for (j=1;j<=M-1;j++) {
    for (k=j+1;k<=M;k++) {
      if ((j <= k-1) && (k <= M)) {
        S2(i = M,l = k,m = M) ;
      }
    }
  }
}
