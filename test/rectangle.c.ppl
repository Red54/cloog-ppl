/* Generated from ./rectangle.cloog by CLooG  64 bits in 0.01s. */
for (c1=0;c1<=2*n;c1++) {
  for (i=max(c1-n,0);i<=min(c1,n);i++) {
    if ((c1 >= i) && (c1 <= i+n)) {
      j = c1-i ;
      S1 ;
    }
  }
}
