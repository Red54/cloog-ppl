/* Generated from ./gesced3.cloog by CLooG 0.15 64 bits in 0.03s. */
for (c1=M+1;c1<=2*M;c1++) {
  if ((c1 >= M+1) && (c1 <= 2*M) && (M >= 2) && (M <= N)) {
    i = c1-M ;
    S1 ;
  }
}
for (c1=2*M+1;c1<=M+N;c1++) {
  if ((c1 >= 2*M+1) && (c1 <= M+N) && (M >= 2)) {
    i = c1-2*M ;
    S2 ;
  }
  if ((c1 >= 2*M+1) && (c1 <= M+N) && (M >= 2)) {
    i = c1-M ;
    S1 ;
  }
}
for (c1=M+N+1;c1<=2*M+N;c1++) {
  if ((c1 >= M+N+1) && (c1 <= 2*M+N) && (M >= 2) && (M <= N)) {
    i = c1-2*M ;
    S2 ;
  }
}
