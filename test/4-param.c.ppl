/* Generated from ./4-param.cloog by CLooG  64 bits in 0.07s. */
for (i=p;i<=min(m-1,q);i++) {
  S2 ;
}
for (i=max(p,m);i<=min(n,q);i++) {
  S1 ;
  S2 ;
}
for (i=m;i<=min(n,p-1);i++) {
  S1 ;
}
for (i=max(max(q+1,p),m);i<=n;i++) {
  S1 ;
}
for (i=max(max(p,n+1),m);i<=q;i++) {
  S2 ;
}
