/* Generated from ./4-param.cloog by CLooG  64 bits in 0.06s. */
for (i=p;i<=min(q,m-1);i++) {
  S2 ;
}
for (i=max(m,p);i<=min(q,n);i++) {
  S1 ;
  S2 ;
}
for (i=m;i<=min(n,p-1);i++) {
  S1 ;
}
for (i=max(max(m,q+1),p);i<=n;i++) {
  S1 ;
}
for (i=max(max(n+1,p),m);i<=q;i++) {
  S2 ;
}
