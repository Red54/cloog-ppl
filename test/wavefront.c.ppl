/* Generated from ./wavefront.cloog by CLooG  64 bits in 0.02s. */
if ((n >= 1) && (m >= 1)) {
  for (c1=2;c1<=n+m;c1++) {
    for (c2=max(1,c1-m);c2<=min(n,c1-1);c2++) {
      if ((c1 <= c2+m) && (c1 >= c2+1) && (c2 >= 1) && (c2 <= n)) {
        if ((c1 <= c2+m) && (c1 >= c2+1)) {
          j = c1-c2 ;
          S1(i = c2) ;
        }
      }
    }
  }
}
