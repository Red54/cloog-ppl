/* Generated from ./largeur.cloog by CLooG  64 bits in 0.01s. */
for (c1=1;c1<=M;c1++) {
  for (c2=1;c2<=c1;c2++) {
    if ((c1 >= c2) && (c2 >= 1)) {
      if ((c1 >= c2) && (c1 <= M)) {
        S1(i = c2,j = c1) ;
      }
    }
  }
}
