/* Generated from ././reservoir/pingali4.cloog by CLooG  64 bits in 0.04s. */
for (c2=1;c2<=M;c2++) {
  for (c4=1;c4<=M;c4++) {
    if ((c2 <= M) && (c2 >= 1)) {
      if ((c4 <= M) && (c4 >= 1)) {
        S1(i = c2,j = c4) ;
      }
    }
  }
}
for (c2=1;c2<=M;c2++) {
  for (c4=1;c4<=M;c4++) {
    if ((c2 <= M) && (c2 >= 1)) {
      if ((c4 <= M) && (c4 >= 1)) {
        S2(i = c2,j = c4) ;
      }
    }
  }
}
