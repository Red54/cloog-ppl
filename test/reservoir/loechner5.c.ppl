/* Generated from ././reservoir/loechner5.cloog by CLooG  64 bits in 0.06s. */
for (c2=1;c2<=M;c2++) {
  for (c4=1;c4<=M;c4++) {
    for (c6=1;c6<=M;c6++) {
      for (c8=1;c8<=M;c8++) {
        if ((c4 >= 1) && (c4 <= M)) {
          if ((c6 >= 1) && (c6 <= M)) {
            if ((c2 >= 1) && (c2 <= M)) {
              if ((c8 >= 1) && (c8 <= M)) {
                S1(i = c4,j = c6,k = c2,l = c8) ;
              }
            }
          }
        }
      }
    }
  }
}
