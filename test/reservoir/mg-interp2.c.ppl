/* Generated from ././reservoir/mg-interp2.cloog by CLooG  64 bits in 0.33s. */
if ((M >= P+1) && (N >= Q+1)) {
  for (c2=1;c2<=O-1;c2++) {
    for (c4=Q;c4<=N-1;c4++) {
      for (c6=P;c6<=M-1;c6++) {
        if ((c2 >= 1) && (c2 <= O-1)) {
          if ((c4 >= Q) && (c4 <= N-1)) {
            if ((c6 >= P) && (c6 <= M-1)) {
              S1(i = c2,j = c4,k = c6) ;
            }
          }
        }
      }
    }
  }
}
if ((M >= 2) && (N >= Q+1)) {
  for (c2=1;c2<=O-1;c2++) {
    for (c4=Q;c4<=N-1;c4++) {
      for (c6=1;c6<=M-1;c6++) {
        if ((c2 >= 1) && (c2 <= O-1)) {
          if ((c4 >= Q) && (c4 <= N-1)) {
            if ((c6 >= 1) && (c6 <= M-1)) {
              S2(i = c2,j = c4,k = c6) ;
            }
          }
        }
      }
    }
  }
}
if ((M >= P+1) && (N >= 2)) {
  for (c2=1;c2<=O-1;c2++) {
    for (c4=1;c4<=N-1;c4++) {
      for (c6=P;c6<=M-1;c6++) {
        if ((c2 >= 1) && (c2 <= O-1)) {
          if ((c4 >= 1) && (c4 <= N-1)) {
            if ((c6 >= P) && (c6 <= M-1)) {
              S3(i = c2,j = c4,k = c6) ;
            }
          }
        }
      }
    }
  }
}
if ((M >= 2) && (N >= 2)) {
  for (c2=1;c2<=O-1;c2++) {
    for (c4=1;c4<=N-1;c4++) {
      for (c6=1;c6<=M-1;c6++) {
        if ((c2 >= 1) && (c2 <= O-1)) {
          if ((c4 >= 1) && (c4 <= N-1)) {
            if ((c6 >= 1) && (c6 <= M-1)) {
              S4(i = c2,j = c4,k = c6) ;
            }
          }
        }
      }
    }
  }
}
