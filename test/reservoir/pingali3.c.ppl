/* Generated from ././reservoir/pingali3.cloog by CLooG  64 bits in 0.06s. */
for (c2=1;c2<=M;c2++) {
  for (c4=1;c4<=M;c4++) {
    if ((c2 <= M) && (c2 >= 1)) {
      if ((c4 <= M) && (c4 >= 1)) {
        S1(i = c2,j = c4) ;
      }
    }
  }
}
for (c2=1;c2<=M;c2++) {
  for (c4=1;c4<=M;c4++) {
    for (c6=1;c6<=M;c6++) {
      if ((c2 <= M) && (c2 >= 1)) {
        if ((c4 <= M) && (c4 >= 1)) {
          if ((c6 <= M) && (c6 >= 1)) {
            S2(i = c2,j = c4,k = c6) ;
          }
        }
      }
    }
  }
}
