/* Generated from ././reservoir/fusion2.cloog by CLooG  64 bits in 0.08s. */
if ((M >= 1) && (N >= 1)) {
  for (c4=1;c4<=M;c4++) {
    if ((c4 >= 1) && (c4 <= M)) {
      S1(i = 1,j = c4) ;
    }
  }
}
if (M >= 1) {
  for (c2=2;c2<=N;c2++) {
    for (c4=1;c4<=M;c4++) {
      if ((c2 >= 2) && (c2 <= N)) {
        i = c2-1 ;
        if ((c4 >= 1) && (c4 <= M)) {
          S2(j = c4) ;
        }
      }
    }
    for (c4=1;c4<=M;c4++) {
      if ((c2 >= 2) && (c2 <= N)) {
        if ((c4 >= 1) && (c4 <= M)) {
          S1(i = c2,j = c4) ;
        }
      }
    }
  }
}
if ((M >= 1) && (N >= 1)) {
  c2 = N+1 ;
  for (c4=1;c4<=M;c4++) {
    if ((c4 >= 1) && (c4 <= M)) {
      S2(i = N,j = c4) ;
    }
  }
}
