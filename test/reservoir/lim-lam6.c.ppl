/* Generated from ././reservoir/lim-lam6.cloog by CLooG  64 bits in 0.03s. */
if (M >= 1) {
  for (c2=0;c2<=M;c2++) {
    for (c4=1;c4<=M;c4++) {
      if ((c2 >= 0) && (c2 <= M)) {
        if ((c4 >= 1) && (c4 <= M)) {
          S1(i = c2,j = c4) ;
        }
      }
    }
  }
}
if (M >= 1) {
  for (c2=0;c2<=M;c2++) {
    for (c4=1;c4<=M;c4++) {
      if ((c4 >= 1) && (c4 <= M)) {
        if ((c2 <= M) && (c2 >= 0)) {
          S2(i = c4,j = c2) ;
        }
      }
    }
  }
}
