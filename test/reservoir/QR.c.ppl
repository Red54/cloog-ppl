/* Generated from ././reservoir/QR.cloog by CLooG  64 bits in 0.80s. */
if (N >= 1) {
  S1(i = 0) ;
}
if ((M >= 1) && (N >= 2)) {
  for (c4=0;c4<=M-1;c4++) {
    if ((c4 >= 0) && (c4 <= M-1)) {
      S2(i = 0,j = c4) ;
    }
  }
  S3(i = 0) ;
  for (c4=0;c4<=M-1;c4++) {
    if ((c4 >= 0) && (c4 <= M-1)) {
      S4(i = 0,j = c4) ;
    }
  }
  S10(i = 0) ;
  S1(i = 1) ;
  S5(i = 0) ;
}
for (c2=2;c2<=min(M,N-1);c2++) {
  for (c4=c2-1;c4<=N-1;c4++) {
    if ((c2 >= 2) && (c2 <= min(min(c4+1,M),N-1))) {
      i = c2-2 ;
      if ((c2 <= c4+1) && (c4 <= N-1)) {
        S6(j = c4) ;
      }
    }
    for (c6=c2-2;c6<=M-1;c6++) {
      if ((c2 >= 2) && (c2 <= min(min(min(c4+1,c6+2),M),N-1))) {
        i = c2-2 ;
        if ((c2 <= c4+1) && (c4 <= N-1)) {
          if ((c2 <= c6+2) && (c6 <= M-1)) {
            S7(j = c4,k = c6) ;
          }
        }
      }
    }
    if ((c2 >= 2) && (c2 <= min(min(c4+1,M),N-1))) {
      i = c2-2 ;
      if ((c2 <= c4+1) && (c4 <= N-1)) {
        S8(j = c4) ;
      }
    }
    for (c6=c2-2;c6<=M-1;c6++) {
      if ((c2 >= 2) && (c2 <= min(min(min(c4+1,c6+2),M),N-1))) {
        i = c2-2 ;
        if ((c2 <= c4+1) && (c4 <= N-1)) {
          if ((c2 <= c6+2) && (c6 <= M-1)) {
            S9(j = c4,k = c6) ;
          }
        }
      }
    }
  }
  for (c4=c2-1;c4<=M-1;c4++) {
    if ((c2 >= 2) && (c2 <= min(c4+1,N-1))) {
      i = c2-1 ;
      if ((c2 <= c4+1) && (c4 <= M-1)) {
        S2(j = c4) ;
      }
    }
  }
  if ((c2 >= 2) && (c2 <= min(M,N-1))) {
    i = c2-1 ;
    S3 ;
  }
  for (c4=c2-1;c4<=M-1;c4++) {
    if ((c2 >= 2) && (c2 <= min(c4+1,N-1))) {
      i = c2-1 ;
      if ((c2 <= c4+1) && (c4 <= M-1)) {
        S4(j = c4) ;
      }
    }
  }
  if ((c2 >= 2) && (c2 <= min(M,N-1))) {
    i = c2-1 ;
    S10 ;
  }
  if ((c2 >= 2) && (c2 <= min(M,N-1))) {
    S1(i = c2) ;
  }
  if ((c2 >= 2) && (c2 <= min(M,N-1))) {
    i = c2-1 ;
    S5 ;
  }
}
if ((M >= 1) && (M <= N-2)) {
  c2 = M+1 ;
  for (c4=M;c4<=N-1;c4++) {
    i = M-1 ;
    if ((c4 >= M) && (c4 <= N-1)) {
      S6(j = c4) ;
    }
    c6 = M-1 ;
    i = M-1 ;
    if ((c4 >= M) && (c4 <= N-1)) {
      k = M-1 ;
      S7(j = c4) ;
    }
    i = M-1 ;
    if ((c4 >= M) && (c4 <= N-1)) {
      S8(j = c4) ;
    }
    c6 = M-1 ;
    i = M-1 ;
    if ((c4 >= M) && (c4 <= N-1)) {
      k = M-1 ;
      S9(j = c4) ;
    }
  }
  S3(i = M) ;
  S10(i = M) ;
  i = M+1 ;
  S1 ;
  S5(i = M) ;
}
if ((M <= 0) && (N >= 2)) {
  S3(i = 0) ;
  S10(i = 0) ;
  S1(i = 1) ;
  S5(i = 0) ;
}
for (c2=max(2,M+2);c2<=N-1;c2++) {
  for (c4=c2-1;c4<=N-1;c4++) {
    if ((c2 >= max(2,M+2)) && (c2 <= min(c4+1,N-1))) {
      i = c2-2 ;
      if ((c2 <= c4+1) && (c4 <= N-1)) {
        S6(j = c4) ;
      }
    }
    if ((c2 >= max(2,M+2)) && (c2 <= min(c4+1,N-1))) {
      i = c2-2 ;
      if ((c2 <= c4+1) && (c4 <= N-1)) {
        S8(j = c4) ;
      }
    }
  }
  if ((c2 >= max(2,M+2)) && (c2 <= N-1)) {
    i = c2-1 ;
    S3 ;
  }
  if ((c2 >= max(2,M+2)) && (c2 <= N-1)) {
    i = c2-1 ;
    S10 ;
  }
  if ((c2 >= max(2,M+2)) && (c2 <= N-1)) {
    S1(i = c2) ;
  }
  if ((c2 >= max(2,M+2)) && (c2 <= N-1)) {
    i = c2-1 ;
    S5 ;
  }
}
if ((M >= N) && (N >= 2)) {
  c4 = N-1 ;
  i = N-2 ;
  j = N-1 ;
  S6 ;
  for (c6=N-2;c6<=M-1;c6++) {
    i = N-2 ;
    j = N-1 ;
    if ((c6 >= N-2) && (c6 <= M-1)) {
      S7(k = c6) ;
    }
  }
  i = N-2 ;
  j = N-1 ;
  S8 ;
  for (c6=N-2;c6<=M-1;c6++) {
    i = N-2 ;
    j = N-1 ;
    if ((c6 >= N-2) && (c6 <= M-1)) {
      S9(k = c6) ;
    }
  }
  for (c4=N-1;c4<=M-1;c4++) {
    i = N-1 ;
    if ((c4 >= N-1) && (c4 <= M-1)) {
      S2(j = c4) ;
    }
  }
  i = N-1 ;
  S3 ;
  for (c4=N-1;c4<=M-1;c4++) {
    i = N-1 ;
    if ((c4 >= N-1) && (c4 <= M-1)) {
      S4(j = c4) ;
    }
  }
  i = N-1 ;
  S10 ;
  i = N-1 ;
  S5 ;
}
if ((M >= 1) && (N == 1)) {
  for (c4=0;c4<=M-1;c4++) {
    if ((c4 >= 0) && (c4 <= M-1)) {
      S2(i = 0,j = c4) ;
    }
  }
  S3(i = 0) ;
  for (c4=0;c4<=M-1;c4++) {
    if ((c4 >= 0) && (c4 <= M-1)) {
      S4(i = 0,j = c4) ;
    }
  }
  S10(i = 0) ;
  S5(i = 0) ;
}
if ((M == N-1) && (N >= 2)) {
  c4 = N-1 ;
  i = N-2 ;
  j = N-1 ;
  S6 ;
  c6 = N-2 ;
  i = N-2 ;
  j = N-1 ;
  k = N-2 ;
  S7 ;
  i = N-2 ;
  j = N-1 ;
  S8 ;
  c6 = N-2 ;
  i = N-2 ;
  j = N-1 ;
  k = N-2 ;
  S9 ;
  i = N-1 ;
  S3 ;
  i = N-1 ;
  S10 ;
  i = N-1 ;
  S5 ;
}
if ((M <= N-2) && (N >= 2)) {
  c4 = N-1 ;
  i = N-2 ;
  j = N-1 ;
  S6 ;
  i = N-2 ;
  j = N-1 ;
  S8 ;
  i = N-1 ;
  S3 ;
  i = N-1 ;
  S10 ;
  i = N-1 ;
  S5 ;
}
if ((M <= 0) && (N == 1)) {
  S3(i = 0) ;
  S10(i = 0) ;
  S5(i = 0) ;
}
