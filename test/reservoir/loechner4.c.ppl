/* Generated from ././reservoir/loechner4.cloog by CLooG  64 bits in 0.10s. */
for (c2=2;c2<=2*M;c2++) {
  for (c4=1;c4<=M;c4++) {
    for (c6=1;c6<=M;c6++) {
      for (c8=max(c2-M,1);c8<=min(M,c2-1);c8++) {
        if ((c6 <= M) && (c6 >= 1)) {
          if ((c4 >= 1) && (c4 <= M)) {
            if ((c2 <= c8+M) && (c2 >= c8+1) && (c8 <= M) && (c8 >= 1)) {
              if ((c2 <= c8+M) && (c2 >= c8+1)) {
                l = c2-c8 ;
                S1(i = c6,j = c4,k = c8) ;
              }
            }
          }
        }
      }
    }
  }
}
