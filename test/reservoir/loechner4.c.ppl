/* Generated from ././reservoir/loechner4.cloog by CLooG  64 bits in 0.07s. */
for (c2=2;c2<=2*M;c2++) {
  for (c4=1;c4<=M;c4++) {
    for (c6=1;c6<=M;c6++) {
      for (c8=max(1,c2-M);c8<=min(c2-1,M);c8++) {
        if ((c6 >= 1) && (c6 <= M)) {
          if ((c4 >= 1) && (c4 <= M)) {
            if ((c2 >= c8+1) && (c2 <= c8+M) && (c8 >= 1) && (c8 <= M)) {
              if ((c2 >= c8+1) && (c2 <= c8+M)) {
                l = c2-c8 ;
                S1(i = c6,j = c4,k = c8) ;
              }
            }
          }
        }
      }
    }
  }
}
