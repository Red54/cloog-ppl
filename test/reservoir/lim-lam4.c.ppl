/* Generated from ././reservoir/lim-lam4.cloog by CLooG  64 bits in 0.08s. */
if (M >= 2) {
  S1(i = 1,j = 0,k = 0) ;
}
for (c2=2;c2<=2*M-2;c2++) {
  for (c4=max(-c2+1,-M+1);c4<=-1;c4++) {
    if ((c2 >= -c4+1) && (c4 >= -M+1) && (c4 <= -1)) {
      for (i=max(1,c2-M+1);i<=min(c2+c4,M-1);i++) {
        if ((c2 >= -c4+i) && (c2 <= i+M-1)) {
          j = c2+c4-i ;
          if ((c2 <= i+M-1) && (c4 <= -1)) {
            S1(k = -c4) ;
          }
        }
      }
    }
    for (c6=max(-c4,c2-M+1);c6<=min(c2-1,M-1);c6++) {
      if ((c2 >= c6+1) && (c2 <= c6+M-1)) {
        i = c2-c6 ;
        if ((c4 >= -c6) && (c4 <= -1)) {
          j = c4+c6 ;
          if ((c4 <= -1) && (c6 <= M-1)) {
            S2(k = c6) ;
          }
        }
      }
    }
  }
  for (i=max(1,c2-M+1);i<=min(c2,M-1);i++) {
    if ((c2 >= max(2,i)) && (c2 <= i+M-1)) {
      j = c2-i ;
      S1(k = 0) ;
    }
  }
}
