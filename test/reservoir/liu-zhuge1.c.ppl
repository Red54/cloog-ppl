/* Generated from ././reservoir/liu-zhuge1.cloog by CLooG  64 bits in 0.29s. */
if ((M >= 0) && (N >= 0)) {
  for (c2=-4;c2<=min(-1,3*M+N-4);c2++) {
    for (c4=max(c2-3*M+4,0);c4<=min(N,c2+4);c4++) {
      if ((c2 <= min(c4+3*M-4,-1)) && (c2 >= c4-4)) {
        if ((c2+2*c4+1)%3 == 0) {
          i = (c2-c4+4)/3 ;
          if ((c2 <= -1) && (c4 <= N) && (c4 >= 0)) {
            S1(j = c4) ;
          }
        }
      }
    }
  }
}
for (c2=0;c2<=min(N-1,3*M-4);c2++) {
  for (c4=0;c4<=c2;c4++) {
    if ((c2 <= min(N-1,3*M-4)) && (c2 >= c4)) {
      if ((c2+2*c4)%3 == 0) {
        i = (c2-c4)/3 ;
        if ((c2 <= min(N-1,3*M-4)) && (c4 >= 0)) {
          S2(j = c4) ;
        }
      }
    }
    if ((c2 <= min(N-1,3*M-4)) && (c2 >= c4)) {
      if ((c2+2*c4+1)%3 == 0) {
        i = (c2-c4+4)/3 ;
        if ((c2 <= min(N-1,3*M-4)) && (c4 >= 0)) {
          S1(j = c4) ;
        }
      }
    }
  }
  for (c4=c2+1;c4<=min(N,c2+4);c4++) {
    if ((c2 <= min(3*M-4,c4-1)) && (c2 >= max(c4-4,0))) {
      if ((c2+2*c4+1)%3 == 0) {
        i = (c2-c4+4)/3 ;
        if ((c2 <= 3*M-4) && (c2 >= 0) && (c4 <= N)) {
          S1(j = c4) ;
        }
      }
    }
  }
  for (c4=0;c4<=c2;c4++) {
    if ((c2 <= min(N-1,3*M-4)) && (c2 >= c4)) {
      if ((c2+2*c4)%3 == 0) {
        i = (c2-c4)/3 ;
        if ((c2 <= min(N-1,3*M-4)) && (c4 >= 0)) {
          S3(j = c4) ;
        }
      }
    }
  }
}
if (N >= 0) {
  for (c2=N;c2<=3*M-4;c2++) {
    for (c4=0;c4<=N;c4++) {
      if ((c2 >= N) && (c2 <= 3*M-4)) {
        if ((c2+2*c4)%3 == 0) {
          i = (c2-c4)/3 ;
          if ((c2 <= 3*M-4) && (c2 >= N) && (c4 >= 0) && (c4 <= N)) {
            S2(j = c4) ;
          }
        }
      }
      if ((c2 >= N) && (c2 <= 3*M-4)) {
        if ((c2+2*c4+1)%3 == 0) {
          i = (c2-c4+4)/3 ;
          if ((c2 <= 3*M-4) && (c2 >= N) && (c4 >= 0) && (c4 <= N)) {
            S1(j = c4) ;
          }
        }
      }
    }
    for (c4=0;c4<=N;c4++) {
      if ((c2 >= N) && (c2 <= 3*M-4)) {
        if ((c2+2*c4)%3 == 0) {
          i = (c2-c4)/3 ;
          if ((c2 <= 3*M-4) && (c2 >= N) && (c4 >= 0) && (c4 <= N)) {
            S3(j = c4) ;
          }
        }
      }
    }
  }
}
if ((M <= 1) && (M >= 0)) {
  for (c2=0;c2<=3*M+N-4;c2++) {
    for (c4=max(0,c2-3*M);c4<=c2;c4++) {
      if ((c2 <= min(c4+3*M,3*M+N-4)) && (c2 >= c4)) {
        if ((c2+2*c4)%3 == 0) {
          i = (c2-c4)/3 ;
          if ((c2 <= 3*M+N-4) && (c4 >= 0)) {
            S2(j = c4) ;
          }
        }
      }
    }
    for (c4=c2-3*M+4;c4<=min(N,c2+4);c4++) {
      if ((c2 <= c4+3*M-4) && (c2 >= max(c4-4,0))) {
        if ((c2+2*c4+1)%3 == 0) {
          i = (c2-c4+4)/3 ;
          if ((c2 >= 0) && (c4 <= N)) {
            S1(j = c4) ;
          }
        }
      }
    }
    for (c4=max(0,c2-3*M);c4<=c2;c4++) {
      if ((c2 <= min(c4+3*M,3*M+N-4)) && (c2 >= c4)) {
        if ((c2+2*c4)%3 == 0) {
          i = (c2-c4)/3 ;
          if ((c2 <= 3*M+N-4) && (c4 >= 0)) {
            S3(j = c4) ;
          }
        }
      }
    }
  }
}
for (c2=max(3*M-3,N);c2<=3*M+N-4;c2++) {
  for (c4=max(c2-3*M,0);c4<=c2-3*M+3;c4++) {
    if ((c2 <= min(c4+3*M,3*M+N-4)) && (c2 >= max(c4+3*M-3,N))) {
      if ((c2+2*c4)%3 == 0) {
        i = (c2-c4)/3 ;
        if ((c2 >= N) && (c2 <= 3*M+N-4) && (c4 >= 0)) {
          S2(j = c4) ;
        }
      }
    }
  }
  for (c4=c2-3*M+4;c4<=N;c4++) {
    if ((c2 <= c4+3*M-4) && (c2 >= max(N,3*M-3))) {
      if ((c2+2*c4)%3 == 0) {
        i = (c2-c4)/3 ;
        if ((c2 >= max(3*M-3,N)) && (c4 <= N)) {
          S2(j = c4) ;
        }
      }
    }
    if ((c2 <= c4+3*M-4) && (c2 >= max(N,3*M-3))) {
      if ((c2+2*c4+1)%3 == 0) {
        i = (c2-c4+4)/3 ;
        if ((c2 >= max(3*M-3,N)) && (c4 <= N)) {
          S1(j = c4) ;
        }
      }
    }
  }
  for (c4=max(c2-3*M,0);c4<=N;c4++) {
    if ((c2 <= min(3*M+N-4,c4+3*M)) && (c2 >= max(N,3*M-3))) {
      if ((c2+2*c4)%3 == 0) {
        i = (c2-c4)/3 ;
        if ((c2 >= max(3*M-3,N)) && (c2 <= 3*M+N-4) && (c4 <= N) && (c4 >= 0)) {
          S3(j = c4) ;
        }
      }
    }
  }
}
if (M >= 2) {
  for (c2=3*M-3;c2<=N-1;c2++) {
    for (c4=max(c2-3*M,0);c4<=c2-3*M+3;c4++) {
      if ((c2 <= min(c4+3*M,N-1)) && (c2 >= c4+3*M-3)) {
        if ((c2+2*c4)%3 == 0) {
          i = (c2-c4)/3 ;
          if ((c2 <= N-1) && (c4 >= 0)) {
            S2(j = c4) ;
          }
        }
      }
    }
    for (c4=c2-3*M+4;c4<=c2;c4++) {
      if ((c2 <= min(c4+3*M-4,N-1)) && (c2 >= max(c4,3*M-3))) {
        if ((c2+2*c4)%3 == 0) {
          i = (c2-c4)/3 ;
          if ((c2 <= N-1) && (c2 >= 3*M-3)) {
            S2(j = c4) ;
          }
        }
      }
      if ((c2 <= min(c4+3*M-4,N-1)) && (c2 >= max(c4,3*M-3))) {
        if ((c2+2*c4+1)%3 == 0) {
          i = (c2-c4+4)/3 ;
          if ((c2 <= N-1) && (c2 >= 3*M-3)) {
            S1(j = c4) ;
          }
        }
      }
    }
    for (c4=c2+1;c4<=min(N,c2+4);c4++) {
      if ((c2 <= c4-1) && (c2 >= max(c4-4,3*M-3))) {
        if ((c2+2*c4+1)%3 == 0) {
          i = (c2-c4+4)/3 ;
          if ((c2 >= 3*M-3) && (c4 <= N)) {
            S1(j = c4) ;
          }
        }
      }
    }
    for (c4=max(0,c2-3*M);c4<=c2;c4++) {
      if ((c2 <= min(N-1,c4+3*M)) && (c2 >= max(c4,3*M-3))) {
        if ((c2+2*c4)%3 == 0) {
          i = (c2-c4)/3 ;
          if ((c2 >= 3*M-3) && (c2 <= N-1) && (c4 >= 0)) {
            S3(j = c4) ;
          }
        }
      }
    }
  }
}
if ((M >= 0) && (N >= 0)) {
  for (c2=max(3*M+N-3,0);c2<=3*M+N;c2++) {
    for (c4=max(0,c2-3*M);c4<=min(N,c2);c4++) {
      if ((c2 <= c4+3*M) && (c2 >= max(c4,3*M+N-3))) {
        if ((c2+2*c4)%3 == 0) {
          i = (c2-c4)/3 ;
          if ((c2 >= 3*M+N-3) && (c4 <= N) && (c4 >= 0)) {
            S2(j = c4) ;
          }
        }
      }
    }
    for (c4=max(0,c2-3*M);c4<=min(N,c2);c4++) {
      if ((c2 <= c4+3*M) && (c2 >= max(c4,3*M+N-3))) {
        if ((c2+2*c4)%3 == 0) {
          i = (c2-c4)/3 ;
          if ((c2 >= 3*M+N-3) && (c4 <= N) && (c4 >= 0)) {
            S3(j = c4) ;
          }
        }
      }
    }
  }
}
