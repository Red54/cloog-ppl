/* Generated from ././reservoir/pingali6.cloog by CLooG  64 bits in 0.08s. */
if (N >= 3) {
  for (c4=2;c4<=N-1;c4++) {
    for (c6=2;c6<=N-1;c6++) {
      if ((c4 >= 2) && (c4 <= N-1)) {
        if ((c6 >= 2) && (c6 <= N-1)) {
          S1(i = 1,j = c4,k = c6) ;
        }
      }
    }
  }
}
if (N >= 3) {
  for (c2=3;c2<=2*M;c2++) {
    for (c4=2;c4<=N-1;c4++) {
      for (c6=2;c6<=N-1;c6++) {
        if ((c2 >= 3) && (c2 <= 2*M)) {
          if (c2%2 == 0) {
            if ((c4 >= 2) && (c4 <= N-1)) {
              if ((c6 >= 2) && (c6 <= N-1)) {
                S1(i = c2/2,j = c4,k = c6) ;
              }
            }
          }
        }
      }
    }
    for (c4=2;c4<=N-1;c4++) {
      for (c6=2;c6<=N-1;c6++) {
        if ((c2 >= 3) && (c2 <= 2*M)) {
          if ((c2+1)%2 == 0) {
            i = (c2-1)/2 ;
            if ((c4 >= 2) && (c4 <= N-1)) {
              if ((c6 >= 2) && (c6 <= N-1)) {
                S2(j = c4,k = c6) ;
              }
            }
          }
        }
      }
    }
  }
}
if (N >= 3) {
  c2 = 2*M+1 ;
  for (c4=2;c4<=N-1;c4++) {
    for (c6=2;c6<=N-1;c6++) {
      if ((c4 >= 2) && (c4 <= N-1)) {
        if ((c6 >= 2) && (c6 <= N-1)) {
          S2(i = M,j = c4,k = c6) ;
        }
      }
    }
  }
}
