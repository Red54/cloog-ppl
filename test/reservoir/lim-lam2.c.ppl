/* Generated from ././reservoir/lim-lam2.cloog by CLooG  64 bits in 0.02s. */
for (c2=1;c2<=M;c2++) {
  if ((c2 >= 1) && (c2 <= M)) {
    S1(i = c2) ;
  }
}
if (N >= 2) {
  for (c2=1;c2<=M;c2++) {
    for (c4=2;c4<=N;c4++) {
      if ((c2 >= 1) && (c2 <= M)) {
        if ((c4 >= 2) && (c4 <= N)) {
          S2(i = c2,j = c4) ;
        }
      }
    }
  }
}
if (N >= 2) {
  for (c2=1;c2<=M;c2++) {
    for (c4=1;c4<=N-1;c4++) {
      if ((c2 >= 1) && (c2 <= M)) {
        if ((c4 >= 1) && (c4 <= N-1)) {
          S3(i = c2,j = c4) ;
        }
      }
    }
  }
}
